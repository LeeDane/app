<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="12dp"
    android:orientation="vertical">
    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/mood_detail_swipe_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
        <ListView
            android:id="@+id/mood_detail_listview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </ListView>
    </android.support.v4.widget.SwipeRefreshLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_alignBottom="@id/mood_detail_swipe_listview"
        android:gravity="center_vertical"
        android:orientation="horizontal">
        <EditText
            android:id="@+id/mood_detail_comment_or_transmit_text"
            android:layout_width="0dp"
            android:layout_weight="9"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:hint="@string/comment_hint"
            android:layout_alignParentBottom="true"/>
        <ImageView
            android:id="@+id/mood_detail_comment_or_transmit_send"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:src="@mipmap/send_mood"
            android:layout_height="wrap_content" />
    </LinearLayout>

</RelativeLayout>